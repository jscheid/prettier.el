(progn
  (require 'lsp-mode)
  (lsp-workspace-folders-add default-directory)

  (let ((exec-path
         (append exec-path
                 (concat default-directory "node_modules/.bin"))))
    (noflet ((featurep (x) nil))
            (web-mode)
            (lsp))))

;; Local Variables:
;; mode: emacs-lisp
;; End:
